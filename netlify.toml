[build]
  command = "pnpm install && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Redirect all non-file requests to React app (app.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
